# Dependencies for package-installer skill
# These are the packages required for the skill itself to function

# Core dependencies
requests>=2.28.0
packaging>=21.0
click>=8.0.0
colorama>=0.4.4

# Security and validation
aiohttp>=3.8.0
psutil>=5.9.0
cryptography>=3.4.8

# Data processing
pyyaml>=6.0
toml>=0.10.2

# Optional: for enhanced security
# pip-audit>=2.4.0
# safety>=2.3.0